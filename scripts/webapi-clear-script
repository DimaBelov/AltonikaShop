WEBAPIPID=$(lsof -i :9092 | grep LISTEN > net.info && awk '{pid=$2; print pid}' net.info) ;
echo "$WEBAPIPID" ;
kill "$WEBAPIPID" ;

mkdir -p altonika-shop/webapi ;
cd altonika-shop/webapi ;
rm -r ./* ;

/bin/bash -c 'exit'